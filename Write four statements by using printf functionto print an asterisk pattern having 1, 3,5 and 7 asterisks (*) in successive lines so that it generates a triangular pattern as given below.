Write four statements by using printf functionto print an asterisk pattern having 1, 3,5 and 7 asterisks (*) in successive lines so that it generates a triangular pattern as given below.
   *
 * *  *
* * * * *
* * * * * * *
program:

#include <stdio.h> 
int main() 
{ 
    int rows = 5; 
    for (int i = 0; i < rows; i++) { 

        for (int j = 0; j < 2 * (rows - i) - 1; j++) { 

            printf(" "); 
        } 
        for (int k = 0; k < 2 * i + 1; k++) { 

            printf("* "); 
        } 
        printf("\n"); 
    } 
    return 0;
}

