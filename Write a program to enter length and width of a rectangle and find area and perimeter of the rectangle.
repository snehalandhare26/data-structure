Write a program to enter length and width of a rectangle and find area and perimeter of the rectangle.
program:

#include <stdio.h>
int main()
{
    float area ,length, width, perimeter;

    printf("Enter length of the rectangle: ");
    scanf("%f", &length);
    printf("Enter width of the rectangle: ");
    scanf("%f", &width);

    area = length * width;

    perimeter = 2 * (length + width);

    printf("Area of the rectange = %f",area);
    printf("Perimeter of rectangle = %f  ", perimeter);

    return 0;
}
